import React from 'react'
import styled from 'styled-components';

function FAQPage({setShowFooter}) {

    setShowFooter(true);
    return (
        <Container>
            
        </Container>
    )
}

export default FAQPage

const Container = styled.div``
